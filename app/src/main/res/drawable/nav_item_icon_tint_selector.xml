<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Cor para o ícone ativo (selecionado) -->
    <item android:color="@color/nav_item_icon_color_active" android:state_checked="true"/>
    <!-- Cor para o ícone inativo (não selecionado) -->
    <item android:color="@color/nav_item_icon_color_inactive"/>
</selector>
