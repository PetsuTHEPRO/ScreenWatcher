<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Cor para o texto ativo (selecionado) -->
    <item android:color="@color/nav_item_text_color_active" android:state_checked="true"/>
    <!-- Cor para o texto inativo (não selecionado) -->
    <item android:color="@color/nav_item_text_color_inactive"/>
</selector>
