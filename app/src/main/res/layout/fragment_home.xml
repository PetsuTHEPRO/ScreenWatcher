<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#1E1E2A"
    android:gravity="center"
    android:orientation="vertical"
    android:padding="5dp">

    <!-- Ícone de Status (será alterado programaticamente) -->
    <ImageView
        android:id="@+id/status_icon"
        android:layout_width="120dp"
        android:layout_height="120dp"
        android:src="@drawable/ic_screen_off"
        android:contentDescription="Ícone de status da tela"
        />

    <!-- Texto de Status -->
    <TextView
        android:id="@+id/status_text"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="16dp"
        android:text="Tela Desligada"
        android:textColor="#FF5252"
        android:textSize="20sp"
        android:textStyle="bold" />

    <!-- Detalhes Adicionais -->
    <TextView
        android:id="@+id/status_details"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp"
        android:text="Inativa desde 10:30 AM"
        android:textColor="#AAAAAA"
        android:textSize="14sp" />

    <!-- Botão de Ação -->
    <Button
        android:id="@+id/action_button"
        android:layout_width="200dp"
        android:layout_height="50dp"
        android:layout_marginTop="32dp"
        android:backgroundTint="#6200EE"
        android:text="Atualizar"
        android:textAllCaps="false"
        android:textColor="#FFFFFF" />

</LinearLayout>